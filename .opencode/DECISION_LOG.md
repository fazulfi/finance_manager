# DECISION_LOG.md — Architectural Decisions

> All significant technical decisions are logged here. Updated by orchestrator after each decision.

| Date       | Decision                                                                                                           | Reason                                                                                                                                                                               | Affected Files                                                      |
| ---------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| 2026-04-12 | Create dedicated @finance/types package for shared TypeScript types and Zod schemas                              | Centralizes type definitions across web and mobile apps, eliminates Prisma direct dependencies in UI code, provides compile-time type safety for forms and API procedures              | `packages/types/`                                                    |
| 2026-04-12 | Create dedicated @finance/utils package for pure utility functions                                                | Centralizes pure utility logic (currency, date, number, validation, calculations) across web and mobile apps; enables type-safe helper functions without framework dependencies; maintains test coverage and unit test infrastructure | `packages/utils/`                                                    |
| 2026-04-12 | Use shared `objectId` Zod regex (`/^[0-9a-fA-F]{24}$/`) for all MongoDB ID inputs — defined once in `trpc.ts`, imported by all routers | Single source of truth for ID format validation; prevents inconsistent regex patterns; rejects malformed IDs at the input layer before any DB call | `packages/api/src/trpc.ts`, all router files in `packages/api/src/routers/` |
| 2026-04-12 | Keep `findFirst` ownership check AND add `userId` to `update`/`delete` WHERE (belt-and-suspenders defense-in-depth) | `findFirst` alone with bare `{ id }` update is vulnerable to TOCTOU race; `userId` in WHERE makes the mutation itself ownership-scoped; `findFirst` still provides user-friendly `NOT_FOUND` errors | All 9 router files in `packages/api/src/routers/` |
| 2026-04-12 | Defer `select` clauses to a separate task (not applied during security hardening) | Adding `select` to every query touches return types across all procedures — too many changes bundled with security fixes; risk of introducing regressions; tracked as tech debt | N/A (deferred) |
| 2026-04-12 | Budget category matching uses `item.name` vs `transaction.category` (string comparison) — TODO for future categoryId relation | Current Transaction model has `category: String` not `categoryId: ObjectId`; adding the relation requires a schema migration and data migration; documented as future refactor | `packages/api/src/routers/budget.ts`, `packages/db/prisma/schema.prisma` |
| 2026-04-12 | Rename `apiClient` to `createApiClient` in `react.tsx` to fix client type mismatch | `createTRPCClient` returns a vanilla client incompatible with React Query hooks; `api.createClient()` returns the correct typed client for `@trpc/react-query` usage | `packages/api/src/react.tsx` |
| 2026-04-12 | Docs agent git sync stages all non-ignored repo changes (not docs-only) after reviewing candidate paths | Session commits should include the full code/doc work, not just README/CHANGELOG/AGENTS/DECISION_LOG; staged-path review plus sensitive-file guard reduces accidental secret pushes while preserving `.gitignore` exclusions | `.opencode/agents/docs.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Current planner output is persisted verbatim to `.opencode/plans/current-plan.md` and overwritten fully on each new draft/revision; planner may read `.env` files for planning but must redact secrets | Prevent drift between planner output and execution handoff while still allowing config-aware plans; a single overwritten plan file avoids stale-plan accumulation, and env reads improve accuracy without exposing raw secret values | `.opencode/agents/planner.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `.opencode/plans/current-plan.md`, `CHANGELOG.md` |
| 2026-04-12 | Planner discovery depth is explicit and reviewer-enforced for non-trivial tasks | Claude-style plans require broad repo grounding, not prompt-only planning; minimum evidence depth reduces mismatch and cuts re-discovery by researcher/coder | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Planner discovery now follows a task-specific coverage matrix, not just file-count minimums | Counting files alone still misses critical surfaces; coverage-by-task-type better matches Claude-style reconnaissance and produces plans that survive handoff without re-discovery | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Superseded later the same day: plan review reads inline planner output and `.opencode/plans/current-plan.md` becomes authoritative only after approval/persistence | This was an intermediate workflow change to address reviewer confusion before the simpler write-first flow replaced it; kept here as history only | `.opencode/agents/orchestrator.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Simplified planning flow: orchestrator writes `.opencode/plans/current-plan.md` before review, and reviewer reads that file as the latest draft | Writing the plan first is simpler operationally, removes reviewer confusion about missing plan artifacts, and keeps one stable file that is overwritten on every planner revision until approval | `.opencode/agents/orchestrator.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `.opencode/plans/current-plan.md`, `CHANGELOG.md` |
| 2026-04-12 | Planning now has an explicit integrity gate: planner must use only real repo agent names and repo-truthful claims, orchestrator sanity-checks the persisted plan, and reviewer rejects invented agents/stale task bodies/contradictions | The failure mode was not just shallow planning but invalid execution metadata; catching stale plan bodies, fake subagent names, uncovered acceptance criteria, and create-vs-modify contradictions before execution makes plans survivable and closer to Claude-style execution specs | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Planner output format now targets Claude Code-style plan bodies, with execution metadata moved into supporting sections instead of dominating the main document | The user wants `current-plan.md` outputs to feel like Claude's rich execution specs, not expanded todo lists; keeping the human-facing body focused on context, dependencies, files, implementation order, key notes, and verification improves readability while preserving reviewer/orchestrator needs in appendices | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Orchestrator execution briefings now mirror the plan's richness as mini-plan prompts for each subagent step | Thin prompts caused subagents to lose rationale, scope boundaries, and success checks between planning and execution; carrying forward request summary, why-now context, owned required outcomes, file scope, verification target, and handoff details makes each delegated step far less ambiguous | `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Manual user upsert in NextAuth `signIn` callback (NOT using @auth/prisma-adapter) | Finance app has its own `Account` model occupying `accounts` collection; using Prisma adapter would create duplicate/conflicting accounts collection; JWT strategy avoids adapter entirely | `apps/web/auth.ts` |
| 2026-04-12 | All `cn()` imports in `packages/ui` use relative paths (not `@/lib/utils` alias) | Relative paths work regardless of tsconfig path alias configuration; avoids mismatch between files using `@/` and `../` for consistency | All 38 component files in `packages/ui/src/components/` |
| 2026-04-12 | Subdirectory barrel files (dialog/, select/, dropdown-menu/, tabs/) re-export from individual per-component files, not monolithic files | Monolithic files (Dialog.tsx, Select.tsx, DropdownMenu.tsx) coexist with per-component files; barrels use per-component files for clarity and to avoid duplicate export conflicts | `packages/ui/src/components/ui/*/index.ts` |
| 2026-04-12 | Add `Account.description` end-to-end (schema, contracts, router, and UI forms) | Account management scope required user-editable descriptions; keeping schema/types/API/UI aligned prevents partial implementation drift | `packages/db/prisma/schema.prisma`, `packages/types/src/models.ts`, `packages/types/src/forms.ts`, `packages/api/src/routers/account.ts`, `apps/web/components/accounts/AccountForm.tsx` |
| 2026-04-12 | Introduce provider/toast/skeleton infrastructure before account pages | Account pages needed unified loading/error/mutation feedback patterns; shared infra reduced duplicate page-specific state plumbing | `apps/web/app/providers.tsx`, `apps/web/app/layout.tsx`, `packages/ui/src/components/ui/skeleton.tsx`, `packages/ui/src/components/ui/toaster.tsx`, `packages/ui/src/hooks/use-toast.ts` |
| 2026-04-12 | Enforce transfer policy: block inactive-account and cross-currency transfers | Prevents invalid money movement and enforces business invariants server-side so client-side bypasses cannot create inconsistent balances | `packages/api/src/routers/account.ts`, `apps/web/components/accounts/TransferDialog.tsx` |
| 2026-04-12 | Add `extensionAlias` in web Next.js config for workspace `.js` imports | Workspace packages expose TS sources in dev; aliasing `.js` requests to TS/TSX keeps module resolution stable in Next.js builds/type-check | `apps/web/next.config.js` |
| 2026-04-12 | Use core react-native SafeAreaView instead of react-native-safe-area-context for mobile UI | Native SafeAreaView from react-native already used in app skeleton; adding safe-area-context package adds unnecessary overhead for vanilla SafeAreaView usage | apps/mobile/app/ (all screens) |
| 2026-04-12 | Implement Category Management System with default seeding and usageCount tracking | Needed 19 default expense categories for user onboarding; usageCount provides transaction category analytics without additional API calls; simplified UX by tracking usage in schema vs manual counting | `packages/db/prisma/schema.prisma`, `packages/types/src/models.ts`, `packages/api/src/routers/category.ts`, `packages/api/src/routers/auth.ts`, `apps/web/components/categories/`, `apps/mobile/components/categories/`, `apps/web/app/(dashboard)/categories/page.tsx`, `apps/mobile/app/categories.tsx` |
| 2026-04-13 | Canonicalize `Transaction.project` as Project ObjectId string or `null` (no free-text values) | Keeps project tagging relational and ownership-verifiable across API/types/UI; prevents analytics drift caused by inconsistent text labels | `packages/api/src/routers/transaction.ts`, `packages/types/src/api.ts`, `packages/types/src/forms.ts`, `packages/types/src/models.ts` |
| 2026-04-13 | Treat project analytics as derived from tagged `EXPENSE` transactions and keep `Project.spent` as recomputed cache via `updateProgress` | Avoids dual-source accounting and manual drift; analytics/progress stay consistent with transaction ledger while preserving fast project list reads | `packages/api/src/routers/project.ts`, `packages/db/prisma/schema.prisma` |
| 2026-04-13 | On project deletion, untag user-owned transactions (`project = null`) before deleting the project | Prevents orphaned project references on transaction documents and keeps historical transactions intact without forced deletion | `packages/api/src/routers/project.ts` |
| 2026-04-13 | Add transaction compound index `@@index([userId, project, date])` for project analytics/filter workloads | Project dashboards and filtered transaction queries frequently scope by user + project + date; compound index reduces scan cost | `packages/db/prisma/schema.prisma` |
| 2026-04-13 | Budget Management System uses BudgetItem embedded type with spent field | Enables efficient time-bound budget storage (weekly/monthly) within budget document; spent field supports progress tracking without additional transaction queries | `packages/db/prisma/schema.prisma`, `packages/types/src/models.ts` |
| 2026-04-13 | Budget period validation enforces type/date alignment (monthly budgets must span same month) | Prevents invalid budget periods (e.g., weekly budget spanning months); improves data quality for time-bound budgets | `packages/api/src/routers/budget.ts` |
| 2026-04-14 | Debt analytics procedures accept nested debt payloads and reuse shared `@finance/utils` helpers instead of hitting the database again | Analytics math is deterministic and purely numerical, so the router can just validate nested payloads and delegate to shared helpers rather than issuing extra Prisma reads | `packages/api/src/routers/debt.ts`, `packages/types/src/api.ts`, `packages/utils/src/calculations.ts`, `packages/utils/src/__tests__/calculations.test.ts` |
| 2026-04-14 | Debt analytics inputs are bounded (`maxMonths`, debt array caps) and reuse shared `objectId` validation so expensive schedules terminate predictably | Guardrails keep long-running payment schedule/snowball calculations within acceptable limits and reuse the existing ID schema instead of new patterns | `packages/api/src/routers/debt.ts`, `packages/types/src/api.ts` |
| 2026-04-14 | Debt due-date clearing is performed via explicit `dueDate: null` updates so the procedure can tell a clear request apart from an omitted field | Requiring `null` for resets removes ambiguity between "leave the due date as-is" and "clear the stored value" | `packages/api/src/routers/debt.ts`, `apps/web/components/debts/DebtForm.tsx` |
| 2026-04-14 | `@finance/api` keeps its debt tests package-local by adding Vitest wiring (script, devDependency, config) instead of pulling UI test runners | API-only packages should not pull browser-focused test harnesses; Vitest stays server-safe while letting the router run deterministic tests | `packages/api/package.json`, `packages/api/vitest.config.ts`, `packages/api/src/__tests__/debt.test.ts` |
