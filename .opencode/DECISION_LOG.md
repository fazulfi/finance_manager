# DECISION_LOG.md — Architectural Decisions

> All significant technical decisions are logged here. Updated by orchestrator after each decision.

| Date       | Decision                                                                                                           | Reason                                                                                                                                                                               | Affected Files                                                      |
| ---------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| 2026-04-12 | Create dedicated @finance/types package for shared TypeScript types and Zod schemas                              | Centralizes type definitions across web and mobile apps, eliminates Prisma direct dependencies in UI code, provides compile-time type safety for forms and API procedures              | `packages/types/`                                                    |
| 2026-04-12 | Create dedicated @finance/utils package for pure utility functions                                                | Centralizes pure utility logic (currency, date, number, validation, calculations) across web and mobile apps; enables type-safe helper functions without framework dependencies; maintains test coverage and unit test infrastructure | `packages/utils/`                                                    |
| 2026-04-11 | Monorepo with Turborepo + pnpm workspaces                                                                          | Shared code between web and mobile, single pipeline                                                                                                                                  | `turbo.json`, `pnpm-workspace.yaml`                                 |
| 2026-04-11 | MongoDB + Prisma (not PostgreSQL + SQLAlchemy)                                                                     | Blueprint specifies MongoDB; Prisma gives type-safe queries                                                                                                                          | `packages/db/prisma/schema.prisma`                                  |
| 2026-04-11 | tRPC v10 for API layer                                                                                             | End-to-end type safety between Next.js and client, no separate REST schema                                                                                                           | `packages/api/`                                                     |
| 2026-04-11 | NextAuth.js v5 for auth                                                                                            | First-class Next.js App Router support, Prisma adapter available                                                                                                                     | `apps/web/app/api/auth/`                                            |
| 2026-04-11 | shadcn/ui as component base                                                                                        | Unstyled primitives, full ownership of component code, Tailwind-native                                                                                                               | `packages/ui/`                                                      |
| 2026-04-11 | Expo Router (file-based) for mobile                                                                                | Same mental model as Next.js App Router, reduces context switching                                                                                                                   | `apps/mobile/app/`                                                  |
| 2026-04-11 | Turborepo v2 `"tasks"` key (not `"pipeline"`)                                                                      | Turborepo v2 changed the pipeline key from `pipeline` to `tasks`; using `pipeline` triggers deprecation warning and breaks in future versions                                        | `turbo.json`                                                        |
| 2026-04-11 | `noUncheckedIndexedAccess: true` in base tsconfig                                                                  | Finance app handles arrays of transactions, accounts, goals — index access must always be null-checked to prevent runtime errors on missing data                                     | `packages/tsconfig/base.json`                                       |
| 2026-04-11 | `packages/ui` extends `nextjs.json` not `base.json`                                                                | UI package contains JSX/TSX components; `nextjs.json` config provides `jsx: preserve` and browser DOM lib types required for React components                                        | `packages/ui/tsconfig.json`                                         |
| 2026-04-11 | `react-native.json` uses `moduleResolution: Node` not `NodeNext`                                                   | Metro bundler (used by Expo/React Native) does not support NodeNext module resolution; using Node (legacy) resolution is required for Expo SDK 51 compatibility                      | `packages/tsconfig/react-native.json`                               |
| 2026-04-11 | `next-auth@5.0.0-beta.25` pinned to beta                                                                           | NextAuth v5 beta is required for Next.js 14 App Router compatibility; v4 does not support App Router server components and edge runtime                                              | `apps/web/package.json`                                             |
| 2026-04-11 | All intra-workspace deps use `workspace:*` protocol                                                                | pnpm workspace protocol ensures all packages resolve to local workspace copies during development and prevents version drift between workspace packages                              | All `package.json` files with `@finance/*` deps                     |
| 2026-04-11 | Downgraded @tanstack/react-query to ^4.36.1 (v4, not v5)                                                           | tRPC v10 peer dep requires @tanstack/react-query@^4.x; v5 has breaking API changes incompatible with tRPC v10 — must not upgrade tRPC to v11 per AGENTS.md                           | `apps/web/package.json`, `apps/mobile/package.json`                 |
| 2026-04-11 | Pinned react-native-reanimated to ~3.10.1 explicitly                                                               | NativeWind 4 resolved to reanimated 4.3.0 (requires RN 0.81-0.85) via transitive dep; Expo SDK 51 / RN 0.74.5 requires reanimated 3.x — explicit pin overrides transitive resolution | `apps/mobile/package.json`                                          |
| 2026-04-11 | MongoDB via Docker (not native install) for local dev                                                              | Docker provides isolated, reproducible MongoDB 7 environment on Windows; native MongoDB on Windows has PATH/service conflicts and is harder to reset                                 | `docker run finance-mongodb`                                        |
| 2026-04-11 | Write .env files using [System.IO.File]::WriteAllText with UTF8Encoding($false) on Windows                         | PowerShell Set-Content -Encoding UTF8 injects BOM (U+FEFF) which can corrupt dotenv key parsing; System.IO.File with UTF8Encoding(false) writes clean UTF-8                          | `packages/db/.env`                                                  |
| 2026-04-11 | Root `tsconfig.json` uses solution anchor pattern (`files:[]`, `references:[]`) without `composite:true`           | Leaf packages use `noEmit:true` which is incompatible with `composite:true`; Turborepo handles build orchestration instead of `tsc -b`                                               | `tsconfig.json`, `packages/*/tsconfig.json`, `apps/*/tsconfig.json` |
| 2026-04-11 | `prettier-plugin-tailwindcss` excluded from `prettier.config.js`                                                   | Package is not installed in any workspace; would cause a runtime error if referenced; can be added later as a separate step when Tailwind workspaces are wired up                    | `prettier.config.js`                                                |
| 2026-04-11 | ESLint v8 legacy format (`.eslintrc.js` with `module.exports`) used at root — not flat config (`eslint.config.js`) | `packages/eslint-config/index.js` exports ESLint v8 legacy format; mixing legacy and flat config formats would cause incompatibility                                                 | `.eslintrc.js`, `packages/eslint-config/index.js`                   |
| 2026-04-11 | Exclude entire `.opencode/` directory from git tracking (not just individual files)                                 | Keep AI agent workspace files (BLUEPRINT.md, AGENTS.md, DECISION_LOG.md, sessions, etc.) private/local — not publicly visible on GitHub; single directory rule is simpler and future-proof | `.gitignore` (line 70)                                              |
| 2026-04-11 | Docs agent owns git push (Docs Git Sync Gate at Step 7); orchestrator verifies status only                            | Keep docs+git sync in one specialist flow, ensure push status is reported consistently (`GIT COMMIT`/`GIT PUSH`), and avoid orchestrator performing repository push operations directly | `.opencode/AGENTS.md`, `.opencode/agents/docs.md`, `.opencode/agents/orchestrator.md` |
| 2026-04-11 | Deferred `composite:true` in `packages/tsconfig/base.json` to Step 1.3+                                            | `nextjs.json` and `react-native.json` set `noEmit:true` which is incompatible with `composite:true` (TS5062 error); deferring until per-package `outDir` is configured                                                                   | `packages/tsconfig/base.json`, `packages/tsconfig/nextjs.json`, `packages/tsconfig/react-native.json` |
| 2026-04-11 | `import/no-unresolved` rule NOT added to `eslint-config/index.js`                                                   | No source files exist at Step 1.2; rule would fire false positives on workspace aliases only when actual TypeScript imports are linted; defer to when first `src/` files are created                                                      | `packages/eslint-config/index.js`                                  |
| 2026-04-11 | Per-package `.eslintrc.js` created in all 5 shared packages                                                         | `parserOptions.tsconfigRootDir: __dirname` needed for `@typescript-eslint` to resolve types correctly per-package directory; root-only config cascade insufficient when Turbo invokes lint from package directory                         | `packages/db/.eslintrc.js`, `packages/api/.eslintrc.js`, `packages/types/.eslintrc.js`, `packages/utils/.eslintrc.js`, `packages/ui/.eslintrc.js` |
| 2026-04-11 | `apps/web/.eslintrc.js` omits `root:true`                                                                            | Root `.eslintrc.js` at monorepo root already has `root:true`; per-app config without it is the correct shadowed pattern — ESLint walks up the directory tree and stops at the root config                                                | `apps/web/.eslintrc.js`                                                                                                                            |
| 2026-04-11 | `tailwind.config.ts` includes `darkMode: "class"`                                                                    | shadcn/ui convention; zero migration cost now vs painful retrofit when shadcn components are added — class-based dark mode requires a single `dark` class on `<html>`, trivial to add later                                              | `apps/web/tailwind.config.ts`                                                                                                                      |
| 2026-04-11 | `page.tsx` has no navigation links at bootstrap stage                                                                | Bootstrap stage only; dead links to non-existent routes create lint noise and confuse development flow — links will be added when target routes exist                                                                                    | `apps/web/app/page.tsx`                                                                                                                            |
| 2026-04-11 | Explicit `: React.JSX.Element` return type on `RootLayout` and `HomePage`                                            | Fixes TS2742 portability error caused by `@types/react` version split in pnpm store (`18.2.79` from `apps/mobile` vs `18.3.28` from `apps/web`); explicit type annotation resolves ambiguous inference and future-proofs against version drift | `apps/web/app/layout.tsx`, `apps/web/app/page.tsx`                                                                                                |
| 2026-04-11 | `output: "standalone"` in `next.config.js`; EPERM on Windows is environment constraint, not a code bug              | Required for Docker/containerized production deployment; Windows symlink creation requires Developer Mode enabled — not a code defect; CI/CD pipelines on Linux are unaffected                                                            | `apps/web/next.config.js`                                                                                                                          |
| 2026-04-11 | Task tracking uses dual-surface sync: OpenCode UI todos via `todowrite` + `.opencode/TODO.md` mirror               | Prevent divergence where step progress appears in UI before/without file updates (or vice versa); enforce deterministic per-step status transitions across both surfaces for reliable session continuity                                 | `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `.opencode/TODO.md` |
| 2026-04-11 | Superseded task tracking model: `.opencode/TODO.md` is init template; runtime progress source of truth is `todowrite` | Reduce redundant write churn and UI/file mismatch risk during execution; keep `TODO.md` as deterministic task snapshot while step-by-step status lives in session todo state managed by OpenCode UI                                     | `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `.opencode/TODO.md` |
| 2026-04-11 | `withNativeWind` imported from `nativewind/metro` (not `nativewind/dist/metro`)                                     | NativeWind v4 exposes `withNativeWind` at the package-level `nativewind/metro` subpath; importing from `nativewind/dist/metro` is an internal path that breaks on version changes and is not the documented API                         | `apps/mobile/metro.config.js`                                                   |
| 2026-04-11 | `"expo-router/babel"` intentionally excluded from `babel.config.js` for Expo SDK 51                                 | Expo SDK 51 bundles `expo-router/babel` inside `babel-preset-expo`; adding it explicitly in `babel.config.js` would cause duplicate plugin registration errors at Metro startup                                                        | `apps/mobile/babel.config.js`                                                   |
| 2026-04-11 | `useColorScheme` sourced from `nativewind` package (not `react-native`) for NativeWind v4 compatibility             | NativeWind v4 wraps `useColorScheme` to be aware of the NativeWind theme system; using `react-native`'s `useColorScheme` bypasses NativeWind's color scheme context and breaks dark/light token switching                              | `apps/mobile/app/(tabs)/_layout.tsx`                                            |
| 2026-04-11 | `SafeAreaView` from `react-native` core (not `react-native-safe-area-context`) — acceptable for bootstrap           | `react-native-safe-area-context`'s `SafeAreaView` requires a `SafeAreaProvider` ancestor to be set up in the root layout; using core `SafeAreaView` avoids that ceremony at bootstrap stage — will be swapped when layouts are built out | `apps/mobile/app/(tabs)/index.tsx`, `transactions.tsx`, `budget.tsx`, `settings.tsx` |
| 2026-04-11 | `@expo/vector-icons` added as explicit direct dep (`^14.0.0`) in `apps/mobile/package.json`                         | Was only a transitive dep via Expo SDK — explicit pin ensures consistent version resolution, prevents pnpm from deduplicating to an incompatible version, and makes the dep visible in package.json for future audits                   | `apps/mobile/package.json`                                                      |
| 2026-04-11 | `BudgetItem` as embedded Prisma `type` (not `model`) | Budget items have no independent lifecycle; embedding avoids join-equivalent queries for budget displays | `packages/db/prisma/schema.prisma` |
| 2026-04-11 | `Transaction.transferTo` as raw `String? @db.ObjectId` (no Prisma relation) | Avoids circular Account→Transaction→Account Prisma relation; trade-off: no referential integrity on this field | `packages/db/prisma/schema.prisma` |
| 2026-04-11 | PrismaClient singleton on `globalThis` (not `global`) | TypeScript strict mode compatibility; Next.js hot-reload safety | `packages/db/src/index.ts` |
| 2026-04-11 | File-level `/* eslint-disable no-console */` in `seed.ts` | `seed.ts` is a CLI script, not app code; file-level disable is less invasive than modifying shared ESLint config | `packages/db/src/seed.ts` |
| 2026-04-11 | JWT-only NextAuth session strategy — no Prisma adapter `Account` model | Finance `Account` model occupies `accounts` collection; NextAuth OAuth `Account` model would conflict; JWT strategy avoids Prisma adapter account table entirely | `packages/db/prisma/schema.prisma`, `apps/web/auth.ts` (future) |
| 2026-04-11 | `@finance/api` keeps a server-safe root entrypoint; React helpers ship only from `@finance/api/react` | Prevents server consumers from importing client-only React Query helpers transitively and keeps package boundaries explicit across web/server usage | `packages/api/package.json`, `packages/api/src/index.ts`, `packages/api/src/react.tsx` |
| 2026-04-11 | `packages/api/src/trpc.ts` uses package-local generic session/context typing instead of direct NextAuth/Prisma app types | Keeps `packages/api` framework-agnostic and reusable while still allowing callers to inject the real DB client and authenticated session at runtime | `packages/api/src/trpc.ts` |
| 2026-04-12 | Use shared `objectId` Zod regex (`/^[0-9a-fA-F]{24}$/`) for all MongoDB ID inputs — defined once in `trpc.ts`, imported by all routers | Single source of truth for ID format validation; prevents inconsistent regex patterns; rejects malformed IDs at the input layer before any DB call | `packages/api/src/trpc.ts`, all router files in `packages/api/src/routers/` |
| 2026-04-12 | Keep `findFirst` ownership check AND add `userId` to `update`/`delete` WHERE (belt-and-suspenders defense-in-depth) | `findFirst` alone with bare `{ id }` update is vulnerable to TOCTOU race; `userId` in WHERE makes the mutation itself ownership-scoped; `findFirst` still provides user-friendly `NOT_FOUND` errors | All 9 router files in `packages/api/src/routers/` |
| 2026-04-12 | Defer `select` clauses to a separate task (not applied during security hardening) | Adding `select` to every query touches return types across all procedures — too many changes bundled with security fixes; risk of introducing regressions; tracked as tech debt | N/A (deferred) |
| 2026-04-12 | Budget category matching uses `item.name` vs `transaction.category` (string comparison) — TODO for future categoryId relation | Current Transaction model has `category: String` not `categoryId: ObjectId`; adding the relation requires a schema migration and data migration; documented as future refactor | `packages/api/src/routers/budget.ts`, `packages/db/prisma/schema.prisma` |
| 2026-04-12 | Rename `apiClient` to `createApiClient` in `react.tsx` to fix client type mismatch | `createTRPCClient` returns a vanilla client incompatible with React Query hooks; `api.createClient()` returns the correct typed client for `@trpc/react-query` usage | `packages/api/src/react.tsx` |
| 2026-04-12 | Docs agent git sync stages all non-ignored repo changes (not docs-only) after reviewing candidate paths | Session commits should include the full code/doc work, not just README/CHANGELOG/AGENTS/DECISION_LOG; staged-path review plus sensitive-file guard reduces accidental secret pushes while preserving `.gitignore` exclusions | `.opencode/agents/docs.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Current planner output is persisted verbatim to `.opencode/plans/current-plan.md` and overwritten fully on each new draft/revision; planner may read `.env` files for planning but must redact secrets | Prevent drift between planner output and execution handoff while still allowing config-aware plans; a single overwritten plan file avoids stale-plan accumulation, and env reads improve accuracy without exposing raw secret values | `.opencode/agents/planner.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `.opencode/plans/current-plan.md`, `CHANGELOG.md` |
| 2026-04-12 | Planner discovery depth is explicit and reviewer-enforced for non-trivial tasks | Claude-style plans require broad repo grounding, not prompt-only planning; minimum evidence depth reduces mismatch and cuts re-discovery by researcher/coder | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Planner discovery now follows a task-specific coverage matrix, not just file-count minimums | Counting files alone still misses critical surfaces; coverage-by-task-type better matches Claude-style reconnaissance and produces plans that survive handoff without re-discovery | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Superseded later the same day: plan review reads inline planner output and `.opencode/plans/current-plan.md` becomes authoritative only after approval/persistence | This was an intermediate workflow change to address reviewer confusion before the simpler write-first flow replaced it; kept here as history only | `.opencode/agents/orchestrator.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Simplified planning flow: orchestrator writes `.opencode/plans/current-plan.md` before review, and reviewer reads that file as the latest draft | Writing the plan first is simpler operationally, removes reviewer confusion about missing plan artifacts, and keeps one stable file that is overwritten on every planner revision until approval | `.opencode/agents/orchestrator.md`, `.opencode/agents/reviewer.md`, `.opencode/AGENTS.md`, `.opencode/plans/current-plan.md`, `CHANGELOG.md` |
| 2026-04-12 | Planning now has an explicit integrity gate: planner must use only real repo agent names and repo-truthful claims, orchestrator sanity-checks the persisted plan, and reviewer rejects invented agents/stale task bodies/contradictions | The failure mode was not just shallow planning but invalid execution metadata; catching stale plan bodies, fake subagent names, uncovered acceptance criteria, and create-vs-modify contradictions before execution makes plans survivable and closer to Claude-style execution specs | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Planner output format now targets Claude Code-style plan bodies, with execution metadata moved into supporting sections instead of dominating the main document | The user wants `current-plan.md` outputs to feel like Claude's rich execution specs, not expanded todo lists; keeping the human-facing body focused on context, dependencies, files, implementation order, key notes, and verification improves readability while preserving reviewer/orchestrator needs in appendices | `.opencode/agents/planner.md`, `.opencode/agents/reviewer.md`, `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Orchestrator execution briefings now mirror the plan's richness as mini-plan prompts for each subagent step | Thin prompts caused subagents to lose rationale, scope boundaries, and success checks between planning and execution; carrying forward request summary, why-now context, owned required outcomes, file scope, verification target, and handoff details makes each delegated step far less ambiguous | `.opencode/agents/orchestrator.md`, `.opencode/AGENTS.md`, `CHANGELOG.md` |
| 2026-04-12 | Manual user upsert in NextAuth `signIn` callback (NOT using @auth/prisma-adapter) | Finance app has its own `Account` model occupying `accounts` collection; using Prisma adapter would create duplicate/conflicting accounts collection; JWT strategy avoids adapter entirely | `apps/web/auth.ts` |
| 2026-04-12 | All `cn()` imports in `packages/ui` use relative paths (not `@/lib/utils` alias) | Relative paths work regardless of tsconfig path alias configuration; avoids mismatch between files using `@/` and `../` for consistency | All 38 component files in `packages/ui/src/components/` |
| 2026-04-12 | Subdirectory barrel files (dialog/, select/, dropdown-menu/, tabs/) re-export from individual per-component files, not monolithic files | Monolithic files (Dialog.tsx, Select.tsx, DropdownMenu.tsx) coexist with per-component files; barrels use per-component files for clarity and to avoid duplicate export conflicts | `packages/ui/src/components/ui/*/index.ts` |
| 2026-04-12 | Add `Account.description` end-to-end (schema, contracts, router, and UI forms) | Account management scope required user-editable descriptions; keeping schema/types/API/UI aligned prevents partial implementation drift | `packages/db/prisma/schema.prisma`, `packages/types/src/models.ts`, `packages/types/src/forms.ts`, `packages/api/src/routers/account.ts`, `apps/web/components/accounts/AccountForm.tsx` |
| 2026-04-12 | Introduce provider/toast/skeleton infrastructure before account pages | Account pages needed unified loading/error/mutation feedback patterns; shared infra reduced duplicate page-specific state plumbing | `apps/web/app/providers.tsx`, `apps/web/app/layout.tsx`, `packages/ui/src/components/ui/skeleton.tsx`, `packages/ui/src/components/ui/toaster.tsx`, `packages/ui/src/hooks/use-toast.ts` |
| 2026-04-12 | Enforce transfer policy: block inactive-account and cross-currency transfers | Prevents invalid money movement and enforces business invariants server-side so client-side bypasses cannot create inconsistent balances | `packages/api/src/routers/account.ts`, `apps/web/components/accounts/TransferDialog.tsx` |
| 2026-04-12 | Add `extensionAlias` in web Next.js config for workspace `.js` imports | Workspace packages expose TS sources in dev; aliasing `.js` requests to TS/TSX keeps module resolution stable in Next.js builds/type-check | `apps/web/next.config.js` |
| 2026-04-12 | Use core react-native SafeAreaView instead of react-native-safe-area-context for mobile UI | Native SafeAreaView from react-native already used in app skeleton; adding safe-area-context package adds unnecessary overhead for vanilla SafeAreaView usage | apps/mobile/app/ (all screens) |
